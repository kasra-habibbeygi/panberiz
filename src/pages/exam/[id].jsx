// Component
import StartExam from '@/components/pages/exam/start';
import LayoutProvider from '@/components/layout';

const Exam = () => {
    return (
        <LayoutProvider>
            <StartExam />
        </LayoutProvider>
    );
};

export default Exam;
